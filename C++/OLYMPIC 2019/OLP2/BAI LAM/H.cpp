#include <bits/stdc++.h>
using namespace std;
int main(){
	long long n,q,dem=0,res=0;
	cin>>n>>q;
	for (int i=0;i<n;i++) cin>>a[i];
	for (int i=0;i<n-1;i++) 
		if (a[i]>a[i+1]) dem++;
	for (int i=0;i<q;i++){
		(if )
	}
	return 0;
}
