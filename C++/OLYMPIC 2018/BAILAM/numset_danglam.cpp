#include <iostream>
#include <fstream>
#include <string>
using namespace std;
int a[100];
main(){
	string s;
	int sum=0;
	for (int i=0;i<s.size();i++){
		sum+=(int)s[i];
	}
	int i=0;
	while (i<s.size()){
		i++;
		for (int j=0;j<s.size();j++){
			s1=s.erase(j,i);
			for (int k=j;k<)
	}
}
