int perfectSquares(long long a, long long b)
{
    return (floor(sqrt(b)) - ceil(sqrt(a)) + 1);
}
// Dem so luong so chinh phuong tu a toi b;
// https://www.geeksforgeeks.org/find-number-perfect-squares-two-given-numbers/
