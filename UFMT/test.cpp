#include <bits/stdc++.h>

using namespace std;

#define FIO ios_base::sync_with_stdio(false);cin.tie(0);
#define ll long long 
#define N 1000005

int main(){
	FIO
	cout<<(double)2*sqrt(3-sqrt(5))*(3+sqrt(5))*(sqrt(10)-sqrt(2));
	return 0;
}
